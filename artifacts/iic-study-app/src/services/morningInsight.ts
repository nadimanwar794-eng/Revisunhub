import { UniversalAnalysisLog } from '../types';

export const generateMorningInsight = async (_logs: UniversalAnalysisLog[], _settings: any, _onSave: (banner: any) => void): Promise<string> => {
    return "AI insight generation is currently disabled.";
};
